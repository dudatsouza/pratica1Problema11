{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run-Code",
            "type": "shell",
            "command": "make clean && make && make run",
            "group": "build"
        }
    ]
}
